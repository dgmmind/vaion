# Pretty URLs routing to index.php
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /vaion/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>
